const fs = require('fs');
const data = JSON.parse(fs.readFileSync('labs_protocols/python_core/03_dicts_sets_tuples.json', 'utf-8'));

const additions = {
  'step-1': " Практика с get() особенно полезна при работе с пользовательским вводом и настройками: программа может получить неполные данные, но всё равно должна работать. Если значение может быть пустой строкой или нулём, заранее реши, какой запасной результат подходит именно для задачи. Метод get() не меняет словарь и не добавляет отсутствующий ключ. Если ключ нужно создать при первом обращении, используй setdefault() или отдельную проверку. Это разные действия: get() только читает, а setdefault() может записать значение.\n\nСловари удобно сравнивать через ключи и значения, но порядок проверки важен. Сначала убедись, что нужный ключ существует, затем читай данные. Для сложной логики можно хранить в словаре не только простые значения, но и вложенные структуры. Главное — выбирать понятные ключи и не смешивать разные смыслы в одном поле.\n\nМини-практика: создай словарь профиля, прочитай имя через квадратные скобки, город через get() с запасным значением и добавь новый параметр присваиванием. Затем выведи весь словарь. Такой набор действий показывает три базовых способа работы с данными.",
  'step-2': " Практика с keys(), values() и items() помогает не только выводить данные, но и преобразовывать их. Например, из items() можно собрать список строк для отчёта, а из values() — найти максимальную оценку. При этом представление связано с исходным словарём: если словарь изменился после создания представления, при обращении оно покажет актуальные данные. Это удобно, но требует внимательности.\n\nМетод update() принимает другой словарь или последовательность пар. Существующие ключи перезаписываются, новые добавляются. Метод pop() полезен, когда нужно одновременно удалить данные и использовать старое значение. Если ключ может отсутствовать, передай pop() запасное значение, как в get().\n\nМини-практика: создай словарь результатов, выведи все ключи, затем все значения, после чего пройди items() и напечатай фразу для каждого ученика. Добавь один новый результат через update() и удали один старый через pop(). Так ты увидишь разницу между чтением, добавлением и удалением.",
  'step-3': " Практика с множествами особенно полезна при очистке данных. Если список содержит повторяющиеся идентификаторы, преобразование в set() быстро оставит только уникальные значения. Однако результат не сохраняет исходный порядок. Когда порядок важен, проходи исходную коллекцию и добавляй элементы в новый список только при первом появлении, используя множество как вспомогательную память.\n\nОперации множеств можно записывать операторами или методами. Операторы удобны для коротких выражений, а методы иногда читаются понятнее в длинной цепочке действий. Перед операцией убедись, что оба операнда действительно являются множествами. Если данные пришли списком, сначала преобразуй их через set().\n\nМини-практика: создай два множества из списков, найди их пересечение, объединение и разность в обе стороны. Затем посчитай количество уникальных элементов и выведи результат. Сравни вывод нескольких запусков: порядок элементов может отличаться, но состав каждого множества должен оставаться одинаковым.",
  'step-4': " Практика с кортежами помогает выбирать подходящий тип данных. Если запись должна меняться, бери список. Если форма фиксирована и данные не должны случайно измениться, бери кортеж. Кортеж также хорошо подходит для возврата нескольких результатов из функции: вызывающая сторона сразу распаковывает их в понятные переменные.\n\nРаспаковка работает не только с кортежами, но и с другими последовательностями. Количество переменных должно соответствовать количеству элементов, если не используется звёздочка. Звёздочка может появиться только в одной позиции шаблона и собиравает оставшиеся элементы в список. Это удобно для разбора записи, где часть полей имеет переменную длину.\n\nМини-практика: создай кортеж из имени, возраста и города, распакуй его в три переменные и выведи. Затем создай кортеж с одним элементом, проверь его тип и попробуй изменить первый элемент, чтобы увидеть ожидаемую ошибку TypeError. После этого создай новый кортеж с исправленным значением."
};

data.steps.forEach(step => {
  if (additions[step.step_id]) {
    step.tutorial_guidance += additions[step.step_id];
  }
});

fs.writeFileSync('labs_protocols/python_core/03_dicts_sets_tuples.json', JSON.stringify(data, null, 2), 'utf-8');

function countWords(text) {
  const noFences = text.replace(/```[\s\S]*?```/g, '');
  const noMd = noFences.replace(/[*_`#>~\-\[\]()]/g, ' ').replace(/\s+/g, ' ');
  return noMd.trim().split(/\s+/).filter(Boolean).length;
}
data.steps.forEach(step => {
  if (step.phase === 'guided') console.log(step.step_id, countWords(step.tutorial_guidance));
});
