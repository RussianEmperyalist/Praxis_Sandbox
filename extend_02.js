const fs = require('fs');
const data = JSON.parse(fs.readFileSync('labs_protocols/python_core/02_lists.json', 'utf-8'));

const extended = {
  'step-1': "Списки в Python — упорядоченные изменяемые коллекции элементов. Создавать списки можно через квадратные скобки [1, 2, 3] или через конструктор list(). Индексация начинается с нуля: первый элемент имеет индекс 0, второй — 1, и так далее. Отрицательная индексация считает с конца: -1 — последний элемент, -2 — предпоследний. Это удобно когда нужно обратиться к элементам с конца без вычисления длины.\n\n```python\nfruits = ['apple', 'banana', 'cherry']\nprint(fruits[0])   # apple\nprint(fruits[-1])  # cherry\n```\n\n```python\nnumbers = [10, 20, 30, 40, 50]\nprint(numbers[2])   # 30\nprint(numbers[-2])  # 40\n```\n\nСписки поддерживают вложенность — могут содержать другие списки. Доступ к элементам вложенного списка идёт через двойную индексацию: matrix[0][1]. Списки могут содержать элементы разных типов: числа, строки, другие списки, даже функции.\n\nТеперь о частых ошибках. Первая — IndexError при обращении к несуществующему индексу. Правильно: проверяй длину списка через len() перед обращением. Почему: Python не предотвращает выход за границы, он просто выбросит IndexError. Всегда убедись что индекс в диапазоне от -len(list) до len(list)-1. Например, для списка из 3 элементов допустимы индексы 0, 1, 2 и -1, -2, -3.\n\nВторая ошибка — путаешь индексацию с перечислением. Правильно: индексы начинаются с 0, не с 1. Почему: это соглашение, унаследованное от C и многих других языков. Элемент номер 1 в человеческом счёте — это list[0] в Python. Это фундаментальное отличие, к которому нужно привыкнуть.",
  'step-2': "Списки в Python имеют богатый набор методов для изменения. append() добавляет элемент в конец списка — это самый быстрый способ добавить один элемент. insert(index, value) вставляет элемент по указанному индексу, сдвигая остальные вправо. remove(value) удаляет первое вхождение указанного значения по содержимому, не по индексу. pop() удаляет и возвращает элемент по индексу (по умолчанию последний), что удобно для стеков.\n\n```python\nmy_list = []\nmy_list.append('a')\nmy_list.append('b')\nmy_list.append('c')\nmy_list.insert(1, 'x')\nmy_list.pop()\nmy_list.remove('b')\nprint(my_list)  # ['a', 'x']\n```\n\n```python\nnums = [3, 1, 2]\nnums.sort()\nprint(nums)  # [1, 2, 3]\nnums.reverse()\nprint(nums)  # [3, 2, 1]\n```\n\nМетод sort() сортирует список на месте (in-place). Он не возвращает новый список, а изменяет текущий. Если нужен отсортированный список без изменения оригинала, используй sorted(nums). Это важное различие, которое часто приводит к багам.\n\nТеперь о частых ошибках. Первая — забывают что insert() принимает индекс ПЕРВым. Правильно: insert(index, value), не insert(value, index). Почему: это порядок аргументов в определении метода, и перепутать их легко. insert(1, 'x') вставит 'x' на позицию 1, а insert('x', 1) вызовет TypeError.\n\nВторая ошибка — используют remove() для удаления по индексу. Правильно: remove(value) удаляет по значению, а pop(index) удаляет по индексу. Почему: remove('a') удалит первый элемент со значением 'a', а pop(0) удалит элемент с индексом 0. Это разные операции с разным назначением.\n\nТретья ошибка — думают что sort() возвращает новый список. Правильно: sort() меняет список на месте и возвращает None. Почему: если написать new_list = nums.sort(), new_list будет None, а nums останется неизменным. Используй sorted(nums) для создания нового отсортированного списка.",
  'step-3': "Срезы (slicing) — мощная возможность Python для извлечения частей списка. Синтаксис: list[start:stop:step]. start — индекс начала (включительно), stop — индекс конца (НЕ включительно), step — шаг. Если не указать start, берётся начало списка. Если не указать stop — до конца. Шаг по умолчанию равен 1.\n\n```python\nnums = [0, 1, 2, 3, 4, 5]\nprint(nums[1:4])    # [1, 2, 3]\nprint(nums[:3])     # [0, 1, 2]\nprint(nums[2:])     # [2, 3, 4, 5]\nprint(nums[::2])    # [0, 2, 4]\nprint(nums[::-1])   # [5, 4, 3, 2, 1, 0]\n```\n\n```python\nletters = ['a', 'b', 'c', 'd', 'e']\nprint(letters[1:4:2])  # ['b', 'd']\nprint(letters[-3:])    # ['c', 'd', 'e']\n```\n\nСрез [::-1] — классический способ перевернуть список. Он создаёт новый список, не изменяя оригинал. Это работает для любых последовательностей: строк, списков, кортежей. Срезы создают КОПИЮ, а не ссылку — это важное свойство, отличающее их от присваивания.\n\nТеперь о частых ошибках. Первая — путает stop включительно/не включительно. Правильно: stop НЕ включается. Почему: nums[1:4] даёт элементы с индексами 1, 2, 3 — но не 4. Это стандартное соглашение в программировании, удобное потому что start:stop всегда даёт ровно stop-start элементов (при step=1). Это делает расчёты с срезами предсказуемыми.\n\nВторая ошибка — забывают что срез создаёт копию, а не ссылку. Правильно: nums[1:] — это новый независимый список. Почему: изменение среза не меняет оригинал. Но это работает только для срезов, не для присваивания (смотрите step-4 для различий).",
  'step-4': "Важно понимать разницу между copy() и присваиванием в Python. Присваивание (a = b) не создаёт копию списка — оно создаёт ещё одну ссылку на тот же объект в памяти. Изменение a повлияет на b и наоборот. Метод copy() создаёт поверхностную копию — новый список с теми же элементами, но независимый от оригинала.\n\n```python\na = [1, 2, 3]\nb = a        # присваивание — одна ссылка\na.append(4)\nprint(b)     # [1, 2, 3, 4] — b тоже изменился!\n\nc = [1, 2, 3]\nd = c.copy()  # копия — разные объекты\nc.append(4)\nprint(d)      # [1, 2, 3] — d не изменился\n```\n\nДля глубокого копирования вложенных списков используй copy.deepcopy() из стандартного модуля copy. Но для плоских списков (без вложенных элементов) copy() достаточно. Срез [:] и конструктор list() тоже создают поверхностную копию.\n\nТеперь о частых ошибках. Первая — думают что присваивание создаёт копию. Правильно: присваивание создаёт ссылку. Почему: Python работает с объектами, а не с копиями. a = b означает «a и b указывают на один и тот же объект в памяти». Это фундаментальное свойство Python, которое отличается от некоторых других языков.\n\nВторая ошибка — используют = вместо copy() когда нужно именно копирование. Правильно: используй .copy() или [:] для создания независимой копии. Почему: без копирования изменения в одном списке автоматически отражаются в другом, что приводит к трудно обнаружимым багам, особенно в больших программах.\n\nТретья ошибка — путают copy() с deepcopy(). Правильно: copy() — поверхностная копия для плоских списков, deepcopy() — глубокая для вложенных. Почему: для списка списков copy() скопирует внешний список, но внутренние списки будут общими между копией и оригиналом."
};

data.steps.forEach(s => {
  if (extended[s.step_id]) {
    s.tutorial_guidance = extended[s.step_id];
  }
});

fs.writeFileSync('labs_protocols/python_core/02_lists.json', JSON.stringify(data, null, 2), 'utf-8');

const data2 = JSON.parse(fs.readFileSync('labs_protocols/python_core/02_lists.json', 'utf-8'));
data2.steps.forEach(s => {
  if (s.tutorial_guidance) {
    const text = s.tutorial_guidance.replace(/```[\s\S]*?```/g, '').replace(/[*_`#>~\-\[\]()]/g, ' ').replace(/\s+/g, ' ');
    const wc = text.trim().split(/\s+/).filter(w => w.length > 0).length;
    console.log(s.step_id, ':', wc, 'words', wc >= 300 && wc <= 500 ? 'OK' : 'FAIL');
  }
});